doctrine_migrations:
    migrations_paths:
        # namespace is arbitrary but should be different from App\Migrations
        # as migrations classes should NOT be autoloaded
        App\Core\Infrastructure\Doctrine\Migrations: '%kernel.project_dir%/src/Core/Infrastructure/Doctrine/Migrations'
    enable_profiler: '%kernel.debug%'
